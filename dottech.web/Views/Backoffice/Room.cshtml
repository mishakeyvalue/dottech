@model dottech.web.ViewModels.BackofficeRoomViewModel

<h2>Hi!</h2>
<a class="btn btn-info" asp-action="Create">Create</a>

@foreach(var thought in Model.Thoughts)
{
    <h2>@thought.Title</h2>
    <div>@thought.Body</div>
}

