<html>  
    <head>
          <% include shared/_head.ejs %>
    </head>
    <body>

        <h1>Oh, new face!!1</h1>

        <form action="/" method="post">
            <input type="text" name="body">
            <input type="submit">
        </form>

        </br>        


<style>

#toread td:hover {
    cursor: pointer;
}
</style>



<table class="table table-striped table-hover " id="toread">
  <thead>
    <tr>
      <th>#</th>
      <th>Link</th>
      
    </tr>
  </thead>
  <tbody>

<% let counter = 0; %>
<% for(t in tasks) {
  if(!tasks[t].done){  
%>
    <tr class="info" id="<%= tasks[t]._id %>">
      <td><%= counter++; %></td>
      <td> <a href="http://google.com"> <%= tasks[t].task %> </a></td>
    </tr>
<%}}%>

<% counter = 0; %>
<% for(t in tasks) {
  if(tasks[t].done){  
%>
    <tr class="success" id="<%= tasks[t]._id %>">
      <td><%= counter++; %></td>
      <td> <%= tasks[t].task %> </td>
    </tr>
<%}}%>


  </tbody>
</table> 

    </body>
  <script>
    $(document).ready(function() {

        $('#toread tr').click(function() {
            var _id = $(this).attr('id');
            console.log(_id);
            $.ajax({
              type: 'PUT',
              data: 'id=17446&chru=0',
              success: function() { location.reload() },
              error: function(){  location.reload()},
              url: '/'+_id,
              cache:false
            });
          
                
            
        });

    });

    $(function() {
  $('#').click(function() {

  });
});
  </script>
</html>